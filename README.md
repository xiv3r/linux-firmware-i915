# linux-firmware-i915
Fix missing linux i915 firmware from /lib/firmware/i915

# Auto Install
```
sudo apt update && sudo apt install -y wget unzip && wget -qO- https://raw.githubusercontent.com/xiv3r/linux-firmware-i915/refs/heads/main/install.sh | sudo bash
```
